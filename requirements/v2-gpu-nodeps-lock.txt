# Pure-Python/PyTorch-API overlay for the audited CUDA base environment. Install every line with --no-deps
# into python_packages_nodeps. The proven base environment remains the sole
# provider of torch, numpy, pandas, scipy, sklearn, PyG, and torch-scatter.
pykeen==1.11.1
click==8.4.2
tabulate==0.10.0
# Binary build-tool exception: NBFNet's rspmm JIT requires the ninja executable.
# The worker also prepends the proven base venv bin/ to PATH and envcheck asserts
# both this distribution version and executable discovery.
ninja==1.13.0
# Transitive runtime dependency used by both Optuna and Marshmallow. It is not
# installed in the proven base environment, so the overlay must own its pin.
packaging==26.2
class-resolver==0.7.1
click-default-group==1.2.4
dataclasses-json==0.6.7
docdata==0.0.5
more-click==0.1.3
more-itertools==11.1.0
optuna==4.9.0
pystow==0.8.21
torch-max-mem==0.1.4
torch-ppr==0.0.8
alembic==1.18.5
colorlog==6.10.1
Mako==1.3.12
marshmallow==3.26.2
SQLAlchemy==2.0.51
typing-inspect==0.9.0
mypy-extensions==1.1.0
greenlet==3.5.3
backports-zstd==1.6.0
